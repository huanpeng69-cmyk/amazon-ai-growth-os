fastapi>=0.110
uvicorn[standard]>=0.27
sqlalchemy>=2.0
pydantic>=2.5
python-multipart>=0.0.9
# 生产环境可追加：
# langgraph>=0.2          # 替换内置轻量编排引擎
# pgvector>=0.2           # PostgreSQL 向量检索
# openai>=1.0             # 接入真实 LLM 生成建议文案
