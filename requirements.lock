# ============================================================
# Amazon AI Growth OS · 生产依赖锁定（可复现构建）
# 目标平台：Docker python:3.13-slim (linux/amd64)
#
# 说明：本文件锁定与 requirements.txt 的 `>=` 下限兼容的、当前公共 PyPI
# 上存在的稳定版本，用于让 `docker build` 结果可复现、避免依赖漂移。
# 重新生成（推荐用 pip-tools）：
#     pip install pip-tools
#     pip-compile requirements.txt -o requirements.lock
# 或在目标运行环境直接：  pip freeze > requirements.lock
# ============================================================

# ---- Web 框架 / ASGI 服务器 ----
fastapi==0.115.6
starlette==0.41.3
uvicorn[standard]==0.34.0
gunicorn==23.0.0

# ---- 数据校验 / ORM ----
pydantic==2.10.4
pydantic-core==2.27.2
annotated-types==0.7.0
SQLAlchemy==2.0.36
greenlet==3.1.1

# ---- 数据库迁移（Alembic，替代手写 _migrate；双后端 SQLite/PostgreSQL）----
alembic==1.19.1
Mako==1.4.1                 # Alembic 迁移模板渲染
MarkupSafe==3.0.3           # Mako 依赖

# ---- multipart 上传（利润成本上传接口）----
python-multipart==0.0.20

# ---- uvicorn[standard] 传递依赖 ----
anyio==4.7.0
sniffio==1.3.1
idna==3.10
click==8.1.7
h11==0.14.0
httptools==0.6.4
python-dotenv==1.0.1
PyYAML==6.0.2
uvloop==0.21.0            # 仅 Linux（容器内启用高性能事件循环；Windows 无此包）
watchfiles==1.0.3
websockets==14.1
typing_extensions==4.12.2

# ---- gunicorn 传递依赖 ----
packaging==24.2

# ---- 生产数据库 / 缓存（docker-compose: postgres + redis）----
psycopg2-binary==2.9.10   # PostgreSQL 驱动（DATABASE_URL=postgresql://...）
redis==5.2.1              # 多实例共享限流后端（未来路线，见 app/ratelimit.py 注释）
